#include <bits/stdc++.h>

#include "err.h"


void syserr(const std::string& x) {
    std::cerr<<"ERROR: "<<x<<std::endl;
    exit(1);
}

void error(const std::string& x) {
    std::cerr<<"ERROR: "<<x<<std::endl;
}